import 'package:flutter/material.dart';

class AppColors {
  static const Color primary = Color(0xFF4A90E2); // Blue for Rider App
  static const Color secondary = Color(0xFF4A90E2); // Blue
  static const Color accent = Color(0xFFFF9F1C); // Orange for actions (Same as client)
  static const Color surface = Colors.white;
  static const Color background = Color(0xFFF7F8FA); // Light Grey (Same as client)
  static const Color text = Color(0xFF1A1A1A); // Dark Grey (Same as client)
  static const Color textLight = Color(0xFF8B8B97); // Light Grey Text (Same as client)
  static const Color border = Color(0xFFE2E2E2); // Light Border (Same as client)
  static const Color error = Color(0xFFFF4B4B); // Soft Red (Same as client)
  
  // Status Colors
  static const Color success = Color(0xFF00C853);
  static const Color warning = Color(0xFFFFAB00);
  static const Color info = Color(0xFF2196F3);
}
